const initialResumeState = {
  personalData: {},
  educationalData: [],
  workExperienceData: [],
};

const initialThemeState = {
  currentTheme: "bg-slate-200",
  allThemes: ["bg-slate-200", "bg-blue-200", "bg-violet-200"],
};

export { initialResumeState, initialThemeState };
